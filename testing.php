<?php

    require_once('config.php');
    
    # query a row
    // $DB->selectArray('xxxxx', 'id = 12');
    
    # get multiple lines
    /*$objSQL = new \lib\dbQ();
    $objSQL ->select('x.*')
            ->from('xxxxxx AS x')
            ->join('yyyyy AS y ON y.id = x.y_id')
            ->add('x.blubb = NOW()');
    while($arrRow = $objSQL->fetch())
    {
        
    }*/
            